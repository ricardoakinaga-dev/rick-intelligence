[project]
name = "rick-storage"
version = "0.1.0"
description = "Typed scope-aware local and S3-compatible object-store adapters"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
