# `packages/providers`

Reserved for typed LLM, embedding, and reranker provider interfaces, retry
policy, correlation IDs, and error taxonomy. Existing provider behavior is
preserved in the Professor and CVG components for now.
