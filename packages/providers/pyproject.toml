[project]
name = "rick-providers"
version = "1.5.0"
description = "Typed async OpenAI-compatible provider boundary and deterministic test/dev provider"
requires-python = ">=3.12"
dependencies = ["rick-contracts", "httpx==0.27.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
