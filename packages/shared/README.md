# `packages/shared`

Reserved for small dependency-light primitives shared by domain packages. It
must not become a service locator, global mutable state container, or home for
business rules owned elsewhere.
