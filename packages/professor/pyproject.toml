[project]
name = "rick-professor"
version = "1.5.0"
description = "Async, evidence-gated Professor orchestration"
requires-python = ">=3.12"
dependencies = ["rick-contracts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
