# `packages/contracts`

Reserved as the canonical serialized contract boundary for users, sessions,
documents, chunks, retrieval evidence, jobs, provider errors, and lock leases.
The existing `rag-contract-v1` document remains the current contract source
until a versioned package is implemented.
