[project]
name = "rick-ingestion"
version = "1.4.0"
description = "Canonical ingestion pipeline (parsers, chunking, jobs, idempotent index)"
requires-python = ">=3.12"
dependencies = ["rick-knowledge"]
