[project]
name = "rick-locking"
version = "1.5.0"
description = "Owner-safe asynchronous lease boundary with in-memory and HTTP backends"
requires-python = ">=3.12"
dependencies = ["rick-contracts", "httpx==0.27.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
