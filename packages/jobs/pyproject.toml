[project]
name = "rick-jobs"
version = "2.1.0"
description = "Canonical durable job contracts for RICK Intelligence runtimes"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
