[project]
name = "rick-decision"
version = "1.0.0"
description = "Deterministic conservative decision layer for grounded responses"
requires-python = ">=3.12"
dependencies = ["rick-evidence", "pydantic==2.6.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
