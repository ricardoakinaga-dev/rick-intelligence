[project]
name = "rick-observability"
version = "0.1.0"
description = "Bounded, redacted observability primitives"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
