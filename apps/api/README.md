# `apps/api`

Reserved for the canonical HTTP entrypoint. The current FastAPI boundary
remains in `cvg-master-rag-v2/src/api/`; this directory is intentionally a
documentation-only placeholder in Phase 1.1.
