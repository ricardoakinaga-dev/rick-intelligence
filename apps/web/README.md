# `apps/web`

Reserved for the canonical native web application. The current validated UI
remains in `cvg-master-rag-v2/frontend/` and is not copied or moved in Phase
1.1. Future work must establish the `/app` clinical workspace and `/admin`
console through a reviewed contract before switching callers.
