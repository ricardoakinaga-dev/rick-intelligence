# State of Art / AAA — recovered execution plan

<!-- engineering-framework: active_action_id=SA-OBSERVABILITY:VERIFY -->

## Purpose / Big Picture

Deliver the full RICK Intelligence root program requested by the user: secure
canonical API and web, durable knowledge ingestion, grounded retrieval, operations,
observability and independently verified AAA quality. The fourteen required
criteria in `.gauntlet-state-of-art/bar.json` remain unchanged. This recovery
repairs continuity; it does not reduce the product objective or certify release.

## Progress

- [x] (2026-09-06T01:48:12Z) App-local provider isolation accepted D1-D6 by fresh I1 Nietzsche; initial D5 evidence rejection preserved and closed by independent48-pass browser run on the unchanged candidate.
- [x] (2026-09-05T21:41:17Z) Archived thirty pre-v2 controller files with SHA256 checks.
- [x] (2026-09-05T21:41:17Z) Validate current controller and historical-reader compatibility; subsequent results in docs/architecture/controller-recovery.md.
- [x] (2026-09-05T22:43:37Z) Independent reviews accepted canonical recovery and recovered review controls; two sidecar rejections were corrected and preserved.
- [x] Strict readiness/login correction independently accepted by fresh I1 Boole after one preserved rejection; scoped VERIFIED gate bound.
- [x] Scoped transport and nested stream ownership accepted by fresh peer Hume;403 combined tests and5 independent probes passed; typed verification and VERIFIED gate bound.
- [x] (2026-09-06T08:28:44Z) Cycle2 UX-polish-fix3 candidate passed two fresh blind I1 critics (96.7 and 96.1) plus distinct Final Critic Ptolemy (98); mutation sentinel remained clean and the scoped visual gate is closed. Full fourteen-criterion acceptance remains open.
- [x] (2026-09-06) The next sealed Cycle3 visual critique was rejected for stale manifest/evidence metadata, missing auditable LCP/CLS values and incomplete skip/reduced-motion disclosure; the rejection is preserved and was not averaged away.
- [x] (2026-09-06T10:25:58Z) Cycle4 production-web candidate passed two fresh blind I1 reviewers: Dalton 97/100 and Kuhn 95.52/100, both HIGH and APPROVE, with zero Critical/High findings; exact hashes, 225/219+6 regression, 108 visual cases, 18 performance records and clean mutation sentinel are bound by the scoped VERIFIED gate.
- [x] (2026-09-06T11:28:00Z) Local security hardening propagated server-derived tenant scope through lifecycle/search and made the default in-memory audit sink bounded/redacted; API root closed at 313 passing tests, with a fresh independent local security review still required.
- [x] (2026-09-06T12:35:06Z) The first independent local security review rejection was preserved, the cookie/tenant/audit corrections were applied, 321 API tests and repository CI passed, and fresh I1 Socrates review accepted the bounded local slice with zero Critical/High/Medium findings; the scoped VERIFIED gate is bound and full external security remains open.
- [x] (2026-09-06T13:09:25Z) Shared observability redaction was hardened for hierarchical URLs across HTTP, non-HTTP and relative forms; 321 API tests, 4 package tests, 29 focused telemetry/transport tests and repository CI passed. The local observability slice remains VERIFY pending a completed I1 review and external collector evidence.
- [x] (2026-09-06T14:17:20Z) Reworked local collection identity, fail-closed journal recovery, reindex postcondition validation and private cleanup ownership after Hegel's four Medium findings; 66 domain, 29 worker/readiness and 326 API tests plus repository CI passed. Fresh McClintock I1 review remains required.
- [x] (2026-09-06T14:44:35Z) Finalized the post-rework service-level upload postcondition and reran the complete hermetic matrix: 66 domain, 29 worker/readiness and 327 API tests plus repository CI passed. McClintock, Russell and Beauvoir were commissioned in fresh context but returned no report and were closed; no I1 acceptance was inferred, so the slice remains VERIFY.
- [x] (2026-09-06T15:10:07Z) Opened the bounded worker-observability implementation gate: wire redacted lifecycle events from both local worker seams into an immutable 256-entry buffer, without claiming exporter, collector or distributed evidence.
- [x] (2026-09-06T15:28:16Z) Implemented and froze the local worker stream: 19 focused tests, reentrant sink probe, Phase 1.6 66/32/327 and preserved-component CI passed; a fresh I1 verdict is still required.
- [x] (2026-09-06T15:35:34Z) The fresh Noether I1 window returned no report after a conclusion request and was closed; the absence is preserved as NOT_RUN and the worker-observability slice remains VERIFY.
- [x] (2026-09-06T15:42:28Z) Opened the superseding root-wiring implementation gate: connect the default API ingestion executor to the app-owned bounded sink while retaining the prior worker I1 NOT_RUN outcome and all external telemetry gaps.
- [x] (2026-09-06T15:56:45Z) Root wiring passed 45 focused tests, Phase 1.6 (66/32/331), preserved-component CI and fallback/reentrant sink probes; the exact expanded artifact is frozen for fresh I1 review.
- [x] (2026-09-06T16:02:42Z) The fresh Faraday I1 window returned no report after a conclusion request and was closed; the absence is preserved as NOT_RUN and the root-wiring slice remains VERIFY.
- [x] (2026-09-06T16:05:02Z) A hermetic fault-injection probe covered root failed and cooperative-cancelled terminal events; both remained bounded/redacted and the raw job/content/error text stayed absent.
- [x] (2026-09-06T16:07:55Z) Sanitized `make api16-verify` passed with `checks_passed=true`, `children_clean=true` and `benchmark_present=true`; no external or I1 acceptance was inferred.
- [x] (2026-09-06T16:18:34Z) Fresh Poincare I1 review rejected the exact root-wiring candidate with HIGH confidence after reproducing `started → published → enqueued` under a fast worker and accepting arbitrary `worker.*` names; the rejection is preserved and no acceptance was inferred.
- [x] (2026-09-06T16:30:31Z) Corrected the root event barrier and finite API worker-event allowlist; focused 45-test regression and direct ordering/allowlist probe passed, with the candidate frozen for a fresh I1 review.
- [x] (2026-09-06T16:37:09Z) The post-rework candidate passed complete Phase 1.6 (`66/32/331` plus benchmark), preserved-component CI and control validation; a fresh I1 review is now running against the exact artifact.
- [x] (2026-09-06T17:30:46Z) A fresh root lifecycle probe reproduced the app-owned ingestion executor surviving ASGI lifespan close (`_shutdown=False`, live `rick-ingestion_0`); a separate implementation gate was opened while the local security candidate remains VERIFY.
- [x] (2026-09-06T17:46:31Z) Installed the app-owned ASGI lifespan shutdown, tracked and cancelled queued futures with private cleanup, preserved injected-provider ownership, and passed focused lifecycle tests, Phase 1.6 (`66/32/337` plus benchmark), preserved CI and the post-rework shutdown probe; candidate V1 is frozen for fresh I1 review.
- [x] (2026-09-06T18:04:12Z) Addressed the independent lifecycle audit's wrapper-ownership, non-cooperative timeout, temporary-root, reentrant-gate and concurrent-shutdown findings; final local evidence passed Phase 1.6 (`66/32/341` plus benchmark), preserved CI and the root probe, with candidate V1 frozen again for fresh I1 review.
- [ ] (2026-09-05T21:41:17Z) Complete full regression and the remaining full-product acceptance after the scoped client-disconnect and Cycle2 visual corrections.
- [ ] (2026-09-05T21:41:17Z) Obtain external runtime, corpus, restore/soak and final release evidence.

## Surprises & Discoveries

The installed controller validator found 264 failures in old schema, gate,
transition and plan records. `make validate` previously checked boundaries only.
The old DONE claims cannot be imported as current verified transitions. Reports
under `.gauntlet-state-of-art/reports/` are narrower evidence, not full approval.
An actual ASGI disconnect probe failed 20/20 times; this remains an open defect.

## Decision Log

2026-09-05, Codex lead: preserve pre-v2 records byte-for-byte in
`.agent/legacy-v1/`, with original-to-archive paths in its manifest. Replace only
the mutable current pointers and start a new observed transition chain. Keep
historical plans intact; do not synthesize old COMPLETE events. Alternatives of
weakening the validator or changing old gate decisions were rejected because
they would erase the distinction between historical claims and current evidence.
Historical CLI consumers now resolve the archived gate location explicitly.

## Outcomes & Retrospective

Control recovery is independently accepted in its bounded scope. Local product work has passed 350 backend/package/worker
tests and 30 browser tests with six viewport-specific skips; these observations
are recorded in scoped reports and require fresh verification when their source
changes. No full AAA score, production release or cross-process guarantee exists.

## Context and Orientation

Repository root is `/home/ricardo/rick-intelligence`. `apps/api`, `apps/web` and
`apps/worker` are canonical surfaces; `packages` holds domain implementations.
The three child repositories are read-only. `.agent/backlog.json` owns current
work status; state owns only pointers; new JSONL ledgers own observed chronology.
The legacy manifest resolves original historical paths without changing bytes.

## Scope and Constraints

The full frozen bar remains in scope. Immediate recovery owns `.agent`, controller
scripts and historical path compatibility, not clinical content or external
deployment. Applicable guidance is `.agent/PLANS.md`, the inspected child
AGENTS instructions, and the named engineering, gauntlet, orchestration and design
skills. Classification is T4_CRITICAL, CROSS_SYSTEM, MEDIUM residual risk for this
reversible local controller migration. No credential creation, production side
effect, child mutation or high-risk acceptance is authorized.

## Architecture and Interfaces

The controller uses schema v2, singular structured actions and typed events. Its
validator is a byte-identical project-local snapshot of the installed framework,
with canonical contracts kept intact. Historical gates leave the active scan
directory but remain hash-checked by `scripts/state_of_art/archive_controller.py`.
The CI entry point composes canonical validation, historical preservation and
existing Git policy; no test success bypasses a failed required control check.

## Milestones

### Milestone 1 — trustworthy continuation

Demonstrate valid state/backlog/plan/log bindings and preservation of all archived
bytes. Require canonical checker PASS, archive integrity PASS, negative tests
rejecting corrupt state/archive, and historical skeleton unit regression PASS.

### Milestone 2 — failure-safe local product

Review readiness/login corrections against their real artifacts, implement a
tested disconnect boundary after an implementation-ready gate, and rerun API,
worker, package and browser regressions. Preserve truthful limitations.

### Milestone 3 — full AAA acceptance

Close every frozen criterion, including WCAG AA and fresh visual score at least
95 at 375/768/1440 px, external durable adapters, approved corpus evaluation,
multi-instance recovery, telemetry delivery, restore, performance/soak and final
independent integration review on the release artifact. Missing evidence blocks
promotion; infrastructure absence does not become PASS.

## Plan of Work

Finish controller recovery first. The near-horizon backlog names bounded review,
disconnect and visual work; distant production milestones remain here until
their prerequisites are available. Add required gates and fresh evidence before
each material implementation activity. Delegate only disjoint bounded surfaces;
the lead exclusively writes control records. Freeze reviewed artifacts and
disable pytest caching during read-only review windows.

## Concrete Steps

From the repository root:

1. [SA-OBSERVABILITY:VERIFY] [ ] Obtain and preserve a fresh read-only I1 review
   of the exact frozen observability candidate V3 manifest, then bind a scoped
   decision only if Critical/High/Medium findings are absent. External export,
   distributed aggregation and full-product/release claims remain open.
2. [SA-INGESTION-APP-LIFECYCLE:IMPLEMENT] [x] Install an application-owned ASGI
   lifespan shutdown for the default root ingestion executor and local
   resources. Preserve caller-owned provider ownership, idempotent cleanup,
   queued-work cancellation and the private staging/journal recovery contract.
   The implementation-ready gate is
   `.agent/gates/state-of-art-ingestion-app-lifecycle-ready.json`.
3. [SA-SECURITY-TENANT-LOCAL:VERIFY] [ ] Obtain and preserve a fresh read-only
   I1 review of the exact frozen candidate V5 report and manifest, then bind a
   scoped gate only if Critical/High/Medium findings are absent. The prior
   Godel rejection remains immutable; external identity/storage and
   full-product security remain open.
4. [SA-SECURITY-TENANT-LOCAL:IMPLEMENT] [x] Correct Godel's four findings in a
   bounded local rework: freeze the exact report/artifact binding, make tenant
   scope mandatory at the KnowledgeStore boundary, enforce collection ACL in
   every admin job path, and preserve a durable private cleanup lease when
   journal write and unlink fail together. Candidate V5 is frozen in the
   canonical report and manifest; complete local regression and CI passed.
4. [x] Bind the current source, production build and all required renders. The
   final packet records `108/108` visual cases, `111` expected functional cases
   with `6` existing skips against the same build; the prior CLS and contrast
   failures remain preserved as observed runs.
3. [x] Execute the first fresh blind pair. Reviewer A rejected84.27 with a
   disputed search-error finding; reviewer B approved95 while identifying
   packet count/path inconsistencies. Preserve both raw verdicts and do not
   average them.
4. [x] Correct the packet metadata and add explicit cross-viewport search-error
   recovery coverage. The corrected build has a fresh `108/108` visual matrix
   and `108` functional passes with `6` existing skips; a new independent pair
   is still required.
5. [x] Execute two fresh blind visual critiques against the sealed UX-polish-fix3
   packet and preserve the95 threshold and prior divergent verdicts. Dewey
   `96.7` and Hypatia `96.1` independently approved with HIGH confidence and no
   Critical/High finding.
6. [x] Run a distinct Final Critic against the same exact packet. Ptolemy
   returned `98/100`, HIGH confidence, APPROVE, zero Critical/High and clean
   integrity; the scoped Cycle2 visual gate is closed.
7. [x] Preserve the subsequent Cycle3 rejection: the fresh critics identified
   stale candidate metadata, non-auditable LCP/CLS values, missing explicit
   reduced-motion/manual disclosure and incomplete skip enumeration. It remains
   a historical rejection and does not weaken the frozen bar.
8. [x] Correct the Cycle4 production-web packet. Add durable per-case LCP/CLS
   evidence, exact performance summarization, explicit reduced-motion and axe
   fields, declared skips, production `next start`/single-worker binding,
   deterministic focus transition handling and compact desktop/tablet chat
   errors. Rebuild and bind source40/build127/render283 to the exact candidate.
9. [x] Obtain two fresh blind I1 reviews of Cycle4 and close the bounded gate.
   Dalton approved `97/100` and Kuhn `95.52/100`, both HIGH, with zero
   Critical/High findings; the protected pre/post sentinel and controller
   closure are required before this record is final.
10. [ ] Close the full frozen bar: external runtime/corpus, durable restore,
    multi-instance, telemetry, performance/soak, deployment and final release
    evidence remain required and are not imported from the scoped visual gate.
11. [x] Complete the fresh read-only review of the local tenant/audit slice;
    continue with external identity/storage and full cross-process security
    evidence. The current local result is not a release approval.

2026-09-05 prioritization: read-only probes confirmed both disconnects counted as
500 and real post-header streaming errors counted as success. This observed
operational correctness gap precedes the still-open readiness/login review.

## Validation and Acceptance

Run `python3 scripts/control_plane/vendor/engineering_framework/scripts/check_state.py .`;
expect RESULT PASS. Run `python3 scripts/state_of_art/archive_controller.py`;
expect thirty matching historical files. Run controller negative tests and
`python3 -m unittest scripts/phase11/test_check_skeleton.py`; expect actual exit 0. `make validate`
must include the controller and boundary checks. Each command result goes into
`.agent/verification.jsonl` with task, scope, observation time and limitations.
Full product acceptance remains the frozen bar, not this narrower recovery suite.

Correction: the original Phase 1.1 CLI intentionally requires a placeholder-only
skeleton and scans later source/generated directories. Running it against the
built product failed; this is retained as a historical scope mismatch, not a
current passing regression. Its unit checks and archived gate bytes are preserved.

## Risks and Human Decisions

Partial archive/switch interruption is controlled by immutable copies, hashes,
single-writer ownership and fail-closed validation. Unknown historical events are
explicitly UNKNOWN. Dirty shared files must not be reset. External runtime and
approved corpus are unavailable; a future authorized environment is required for
their evidence. No human authority is inferred for deployment or residual HIGH
risk acceptance. Current tests do not resolve those missing gates.

## Idempotence and Recovery

Before resuming, read state, this plan, backlog, ledger tails and current reports.
Verify the archive before any repair. Archive creation refuses overwrite of
different bytes; archive verification remains read-only after cutover. If
interrupted, inspect which current pointers changed and repair forward, retaining
the archived originals. Do not blindly restore old state over newer events.
Historical `.agent/gates/X` resolves to `.agent/legacy-v1/gates/X` via the manifest;
new gates belong only in `.agent/gates/`. No external effect is pending.

## Artifacts and Evidence

- `.agent/legacy-v1/manifest.json`: exact pre-v2 archive inventory and hashes.
- `.agent/RECOVERY-CURRENT.md`: diagnostic that triggered migration.
- `.gauntlet-state-of-art/reports/round-4-concurrency-verdict.md`: scoped local review.
- `.gauntlet-state-of-art/reports/readiness-deadline-integration.md`: 350-test observation.
- `.gauntlet-state-of-art/reports/login-navigation-correction.md`: current web evidence.
- `.gauntlet-state-of-art/reports/client-disconnect-diagnostic.md`: reproduced open defect.
- `.gauntlet-state-of-art/reports/visual-cycle3-rejection.md`: historical Cycle3
  rejection and the evidence corrections required by Cycle4.
- `.gauntlet-state-of-art/reports/visual-cycle4-production-web.md`: current
  bounded production-web gate and remaining full-product limitations.

Plan revision 2026-09-06: Cycle2 local copy/recovery and exact source/build/render
binding were completed from actual artifacts. A subsequent Cycle3 rejection is
preserved for stale evidence metadata and missing auditable performance/
accessibility disclosure. Cycle4 now closes the bounded production-web gate
after two fresh blind approvals (Dalton97 and Kuhn95.52), exact 225/219+6 and
108/108 local evidence, and a clean protected mutation sentinel. External and
full-product acceptance remain open. No historical completion or release
decision was fabricated.

2026-09-05 rework: third independent transport rejection is preserved in
`.gauntlet-state-of-art/reports/transport-review-3.md`. Supersede the original
entry gate for outermost observation and bounded retained stream ownership.
Multi-workstream local-write: lead owns streaming.py and test_stream_cleanup.py;
one builder owns app.py, middleware.py, errors.py, transport.py and
test_server_error_transport.py. No shared writers, descendants or external effects.
Shared transport-state interface and admission behavior are specified in the
transport contract. Integration requires both focused suites, full regression,
and a fresh read-only critic. One builder plus one later critic, bounded to this
correction; reassess any new hypothesis before more rework.

Fourth peer rejected nested service cleanup while accepting outer transport and
regression criteria. Lead now owns the coupled helper and both route call sites;
no builders remain active. Preserve report transport-review-4.md, add canonical
route regressions and require another fresh critic after integration.

Transport completion: Hume's fresh sealed I1 approval is bound by
VER-SA-TRANSPORT-PEER-5 and GATE-SA-DISCONNECT-VERIFIED-001. All agent handles
are closed. Next audit owns readiness flags and login ordering, not transport.
Read-only readiness probes in /tmp/rick-readiness-flags-zF5yWj/test_flags.py
previously had10 failures5 passes: truthiness converts malformed ok/required
values into readiness success or optional policy. An in-memory execution of the
actual transpiled SessionProvider with simulated hooks also showed an older
refresh401 erasing a successful new login; this is logic evidence only and still
needs real browser reproduction. No source correction for either is yet gated.

Current audit confirmed both through actual HTTP/Chromium; detailed scope,
acceptance, ownership and proposed implementation are in
docs/architecture/readiness-login-state.md. A separate process-global provider
isolation defect is reproduced and routed to SA-DI-ISOLATION, not accepted or
hidden inside this narrower correction. The temporary diagnostic Next server was
stopped after the browser probe; no external side effect occurred.
